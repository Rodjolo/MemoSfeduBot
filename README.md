# LocalMind - Telegram Bot с локальной LLM

Telegram-бот с интеграцией локальной языковой модели через LM Studio. Реализует систему управления контекстом диалога для персонализированного взаимодействия с каждым пользователем.

## 🎯 Реализованные возможности

### Шаг 2: Базовая интеграция (5 баллов)
- ✅ Подключение к LM Studio через локальное API
- ✅ Обработка сообщений пользователей
- ✅ Генерация ответов с использованием локальной модели

### Шаг 3: Система контекста (10 баллов)
- ✅ Хранение истории диалога для каждого пользователя в словаре Python (ключ = user_id)
- ✅ Независимое управление контекстом для разных пользователей
- ✅ Добавление запросов с меткой "user" в историю
- ✅ Отправка полной истории диалога в LM Studio для генерации ответа
- ✅ Добавление ответов модели с меткой "assistant" в историю
- ✅ Бот явно использует контекст (помнит предыдущие вопросы и отвечает уместно)

### Шаг 4: Очистка контекста (5 баллов)
- ✅ Команда `/clear` для полной очистки истории диалога
- ✅ После очистки бот начинает диалог заново, не ссылаясь на предыдущие сообщения
- ✅ Отображение количества удаленных сообщений

## 📋 Дополнительные возможности

- 🤖 Команда `/help` с подробной справкой
- 📊 Логирование всех действий пользователей
- 🔄 Автоматическая обрезка истории при достижении лимита (предотвращение переполнения токенов)
- ⌨️ Индикатор "печатает..." при генерации ответа
- 📝 Персонализированные приветствия с именем пользователя
- ⚠️ Детальные сообщения об ошибках с инструкциями по устранению

## Требования

- Python 3.8+
- LM Studio (установленный и настроенный)
- Telegram Bot Token (от @BotFather)

## Установка и запуск

### 1. Настройка окружения

Создайте виртуальное окружение и установите зависимости:

```bash
python -m venv venv
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate

pip install -r requirements.txt
```

### 2. Настройка LM Studio

1. Запустите LM Studio.
2. Загрузите модель (например, `Qwen2.5-1.5B-Instruct`).
3. Перейдите во вкладку **Local Server** (значок `<->`).
4. Нажмите **Start Server**. Убедитесь, что сервер запущен на `http://localhost:1234`.

### 3. Настройка бота

1. Откройте файл `.env`.
2. Вставьте ваш токен Telegram-бота (полученный от @BotFather):
   ```
   TELEGRAM_TOKEN=ваш_токен_здесь
   ```

### 4. Запуск бота

```bash
python bot.py
```

Вы увидите сообщение:
```
==================================================
🤖 LocalMind Bot is running...
📡 Connected to LM Studio at http://localhost:1234/v1
✅ Ready to accept messages!
==================================================
```

## 💬 Использование

### Основные команды

- `/start` — начать или перезапустить диалог с ботом
- `/help` — показать справку по использованию
- `/clear` — очистить историю диалога (забыть весь контекст)

### Пример использования контекста

Бот запоминает историю разговора и может отвечать на уточняющие вопросы:

```
Вы: Расскажи про Python
Бот: [развернутый ответ о Python]

Вы: А какие у него преимущества?
Бот: [ответ о преимуществах Python, понимая контекст]

Вы: Где он используется?
Бот: [ответ об областях применения Python]
```

Для начала новой темы используйте `/clear`, и бот забудет предыдущий разговор.

## 📂 Структура проекта

```
Лаба 5/
├── bot.py              # Основной код бота с системой контекста
├── requirements.txt    # Зависимости проекта
├── .env               # Конфигурация (токен Telegram)
└── README.md          # Документация проекта
```

## 🔧 Технические детали

### Архитектура контекста

- **Хранение**: Словарь Python `user_context = {user_id: [messages]}`
- **Формат сообщений**: Список словарей `{"role": "user/assistant/system", "content": "..."}`
- **Лимит истории**: 20 сообщений (автоматическая обрезка)
- **Изоляция**: Каждый пользователь имеет независимую историю

### API интеграция

- **Библиотека**: `openai` (совместима с LM Studio API)
- **Endpoint**: `http://localhost:1234/v1`
- **Параметры**: temperature=0.7, max_tokens=500

## ⚠️ Возможные проблемы

**Ошибка: "Error communicating with LM Studio"**
- Убедитесь, что LM Studio запущен
- Проверьте, что модель загружена
- Нажмите кнопку "Start Server" во вкладке Local Server
- Проверьте доступность `http://localhost:1234`

**Бот не отвечает в Telegram**
- Проверьте правильность токена в `.env`
- Убедитесь, что бот запущен (`python bot.py`)
- Проверьте интернет-соединение

## 📊 Критерии оценки

- ✅ **Шаг 2 (5 баллов)**: Базовая интеграция с LM Studio
- ✅ **Шаг 3 (10 баллов)**: Система контекста с независимым хранением для каждого пользователя
- ✅ **Шаг 4 (5 баллов)**: Команда `/clear` для очистки контекста

**Итого: 20 баллов**
